namespace Spirit604.AnimationBaker
{
    public enum AnimationUseType
    {
        /// <summary> Each entity of the crowd must have this animation.</summary>
        Mandatory,

        /// <summary> Each entity of the crowd doesn't necessarily have to have this animation.</summary>
        Optional,
    }
}
