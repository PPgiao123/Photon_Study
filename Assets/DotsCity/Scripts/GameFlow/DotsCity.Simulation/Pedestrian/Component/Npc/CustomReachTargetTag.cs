using Unity.Entities;

namespace Spirit604.DotsCity.Simulation.Pedestrian
{
    /// <summary>
    /// If the user has a custom reach processing system.
    /// </summary>
    public struct CustomReachTargetTag : IComponentData, IEnableableComponent { }
}
